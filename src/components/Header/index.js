// @flow
import React from 'react';
import SearchBar from '../SearchBar';
import './index.css';

const Header = () => (
  <div className="Header">
    <SearchBar />
  </div>
);

export default Header;
